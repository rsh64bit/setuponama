git config --global user.name "ravi sharma"
git config --global user.email "rsharma.lnx@gmail.com"
git config --global core.editor "vim"
git config --global color.ui auto
ssh-keygen -t rsa -b 4096 -C "rsharma.lnx@gmail.com"
